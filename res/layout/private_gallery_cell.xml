<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/private_gallery_item_width"
    android:layout_height="@dimen/private_gallery_item_height"
    android:id="@+id/cell_item"
    >
	<FrameLayout 
	    android:layout_width="match_parent"
	    android:layout_height="match_parent"
	    android:layout_marginTop="4dip"
	    android:layout_marginRight="4dip"
	    android:layout_marginLeft="4dip">"
        <ImageView
            android:id="@+id/icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />

         <CheckBox
            android:id="@id/checkbox"
            android:layout_width="@dimen/private_chkbox_size"
            android:layout_height="@dimen/private_chkbox_size"
            android:button="@drawable/multi_checkbox"
            android:clickable="false"
            android:focusable="false"
            android:visibility="gone" />
   
	</FrameLayout>
</FrameLayout>